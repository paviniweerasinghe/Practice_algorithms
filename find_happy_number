Find given number is a happy number. 
If repeatedly summing the digits of a number eventually leads to 1, then it's a Happy Number.

    public static boolean isHappy(int num) {        
        while(num >= 10) {
            num = findSum(num);
        }
        
        if (num == 1) {
            return true;
        } else return false;

    }
    
    public static int findSum(int num) {
        int sum = 0;
            while(num > 0) {
            sum = sum + num % 10;
            num = num / 10;
        }
        return sum;
    }

    public static void main(String[] args) {
        System.out.println(isHappy(19));  ------> happy number
        System.out.println(isHappy(192)); ------> Not happy number 
    }
